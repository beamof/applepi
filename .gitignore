# Rust
/target
debug/
*.pdb

# 本地密钥（含真实 key/token，绝不提交）
.env

# 运行时数据（用户记忆库）
/data

# 本地 embedding 模型缓存（candle 下载的 safetensors 权重，体积大，不入库）。
# 默认位置由 hf-hub 决定：~/.cache/huggingface/hub；可在 config.yaml 设 embeddings.cache_dir。
/.fastembed_cache
/model_cache

# 工具临时目录
/.zcode
